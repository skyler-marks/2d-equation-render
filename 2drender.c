#include "raylib.h"
#include <stdio.h>
#include "raymath.h"

# define WIDTH 1000
# define HEIGHT 1000
# define DEPTH 1000


int eqinbox (const char* e, Rectangle r){
	return 0;
}

int main () {
		InitWindow(WIDTH, HEIGHT, "Cloth - Sim");
		SetExitKey(KEY_Q);
	while (!WindowShouldClose()) {
	BeginDrawing();
		ClearBackground(BLACK);
	CloseWindow();
	return 0;
}}

